import { Box } from "@mui/material";
import ManagerAddPromotionContainer from "../../components/manager/ManagerAddPromotionContainer";

export default function ManagerAddNewPromotionContent() {
  return (
    <Box m={2} p={4}>
      <ManagerAddPromotionContainer />
    </Box>
  );
}
